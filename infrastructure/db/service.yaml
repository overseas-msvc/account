apiVersion: v1
kind: Service
metadata:
  name: account-mysql
  labels:
    service: account
spec:
  selector:
    service: account
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP